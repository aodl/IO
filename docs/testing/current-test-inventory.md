# Current Test Inventory

This inventory records proof strength for tests relevant to real SNS and full protocol coverage. It is not a full list of every unit test.

| Test or command | File/crate | Layer | Proves | Does not prove |
| --- | --- | --- | --- | --- |
| `icrc_error_mapping_preserves_duplicate_and_bad_fee`, `icrc_error_mapping_preserves_insufficient_funds_and_generic_error`, `duplicate_transfer_proof_checks_operation_and_ledger_kind_when_available` | `crates/io_ledger_types/src/lib.rs` | Unit | Ledger boundary error and duplicate-proof mapping | Real SNS ledger behavior |
| `account_history_scan_*`, `icrc_index_result_*`, `archive_traversal_requires_complete_contiguous_ranges` | `crates/io_ledger_types/src/lib.rs` | Unit | Index ordering, lag/archive flags, cursor safety | Real SNS index account history |
| `local_sns_*` ledger model tests | `crates/io_ledger_types/src/lib.rs` | Unit | Reserve-transfer issuance/redemption model, constant supply assumptions | Real SNS-created ledger |
| `sns_candid_records_convert_to_domain`, `sns_production_list_neurons_fixture_maps_page_and_malformed_id`, `sns_production_list_proposals_fixture_maps_participation_inputs`, `sns_eligibility_excludes_expected_neurons`, `sns_participation_*` | `crates/io_governance_types/src/lib.rs` | Unit | Production-shaped SNS governance DTO mapping, eligibility, participation | Normal SNS staking, real governance canister rewards |
| `eligible_sns_staking_increases_io_reward_entitlement`, `increasing_staked_io_increases_reward_weight_without_double_counting`, `ineligible_sns_staking_does_not_increase_reward_entitlement` | `crates/io_reward_policy/src/lib.rs` | Unit | IO reward/APY policy response to eligible/ineligible SNS-shaped neuron state and stake increase | Real SNS staking/top-up |
| `governance_source_drives_equal_two_week_allocation`, `participation_and_stake_time_weighting_flow_into_allocation`, `governance_exclusions_and_invalid_ids_are_reported` | `canisters/io_stream_manager/src/governance_snapshot.rs` | Unit | Snapshotting paged SNS-shaped governance data into reward snapshots | Real SNS governance canister |
| `pocketic_live_sns_governance_reads_drive_two_week_allocation` | `tests/pocketic/io_sns_governance_read_pocketic.rs` | SNS-shaped PocketIC | IO reads mock SNS-shaped governance pages in PocketIC and allocates rewards | Real SNS governance, staking, maturity |
| `pocketic_live_two_week_maturity_allocates_io_from_mock_sns_snapshot`, `pocketic_live_nns_manager_maturity_feeds_stream_manager_rewards` | `tests/pocketic/io_stream_manager_pocketic.rs` | SNS-shaped PocketIC | Mock SNS snapshot can drive two-week IO rewards through IO canister stack | Real SNS governance/ledger |
| `pocketic_live_jupiter_faucet_stream_moves_mock_ledger_balances_once`, `pocketic_live_tiny_authorized_icp_deposit_does_not_block_later_valid_deposit`, `pocketic_live_index_lag_blocks_scan_then_resolves_once`, `pocketic_live_archive_required_blocks_redemption_scan_without_mutation` | `tests/pocketic/io_stream_manager_pocketic.rs` | Mock/PocketIC | Deposit scanning, dust rejection, index lag/archive behavior with mock ledgers/indexes | Real ICP/SNS ledger/index |
| `pocketic_live_redemption_pays_icp_and_returns_io_to_reserve_once`, `pocketic_live_redemption_*failure*` | `tests/pocketic/io_stream_manager_pocketic.rs` | Mock/PocketIC | Redemption payout/return retry safety with mock ledgers | Real ledger duplicate/idempotency behavior |
| `pocketic_live_sns_topology_installs_io_canisters_with_local_principals` | `tests/pocketic/io_sns_topology_pocketic.rs` | SNS-shaped PocketIC | IO canisters can be placed in a PocketIC SNS/application topology with local principals | Real SNS root/governance/ledger |
| `pocketic_live_sns_root_*` lifecycle tests | `tests/pocketic/io_sns_root_lifecycle_pocketic.rs` | SNS-shaped PocketIC | Mock SNS root/governance upgrade intent and outcome tracking | Real SNS root proposal execution |
| `e2e_jupiter_to_staking_to_maturity_to_redemption` and related `io_e2e` tests | `tests/e2e/io_e2e.rs` | Mock/model | Full model lifecycle through issuance, staking target, maturity, rewards, unwind, redemption | Any real canister behavior |
| `cargo run -p xtask -- validate_local_sns_rehearsal` | `tools/xtask` | Static/local-manual guard | Local SNS rehearsal package shape and safety | Completed real local SNS run |
| `cargo run -p xtask -- validate_local_sns_ledger` | `tools/xtask` | Official local SNS rehearsal evidence, if file exists | Parses completed local evidence for real local SNS ledger/index/governance/root observations | Does not call canisters; skips if evidence absent |
| `cargo run -p xtask -- local_sns_evidence_tests` | `tools/xtask` | Official local SNS evidence parser | Env-gated parse/policy gate for completed local evidence | No canister calls by default |
| `cargo test -p e2e-real-canisters` | `tests/e2e_real_canisters` | Unit/static harness | Manifest parsing, missing artifact skip semantics, required-artifact failure, SHA-256 mismatch failure | Does not install real canisters without ignored env-gated tests |
| `real_sns_ledger_index_smoke` | `tests/e2e_real_canisters/src/lib.rs` | Real-framework PocketIC, ignored/opt-in | With pinned local SNS ledger/index Wasms: installs real ledger/index, queries ICRC metadata/balances/fee/total supply, transfers reserve-to-user, checks BadFee/InsufficientFunds/Duplicate, verifies index account history and constant supply | Not run in default CI; no governance/root/staking |
| `real_sns_ledger_index_same_wasm_upgrade_preserves_balances_history_and_duplicates` | `tests/e2e_real_canisters/src/lib.rs` | Real-framework PocketIC, ignored/opt-in | With pinned local SNS ledger/index Wasms: same-Wasm ledger/index upgrades preserve balances, history, and duplicate proof behavior | Not run in default CI; archive behavior not induced |
| `real_sns_governance_staking_smoke` | `tests/e2e_real_canisters/src/lib.rs` | Real-framework PocketIC direct-governance proof | With pinned real SNS governance/ledger Wasms: transfers liquid IO into governance staking subaccounts, claims/refreshes a neuron through real `manage_neuron`, verifies `list_neurons`, verifies top-up increases existing stake, verifies below-minimum stake rejection, and observes dissolve delay below and at two weeks | Direct governance install only; no SNS-W deploy/finalize/swap-created neurons, no voting/following |
| `real_nns_*` setup tests | `tests/e2e_real_canisters/src/nns_setup.rs` | Unit/static plus opt-in real SNS-W smoke | Tracks the minimal NNS/SNS-W install plan, well-known canister IDs, required artifact rejection, NNS subnet policy, and protected-ID exclusion. The ignored `real_nns_sns_wasm_canister_responds_to_basic_queries` installs real SNS-W on the NNS subnet and queries real SNS-W readiness methods. The ignored `real_nns_sns_wasm_bootstrapped_by_pocketic_icp_features_contains_all_sns_wasm_slots` uses PocketIC ICP features to initialize registry, ICP ledger, NNS governance/root, and SNS-W, then proves SNS-W reports all six SNS Wasm slots including Governance. | PocketIC ICP-feature bootstrap is not NNS proposal publication proof; finalized SNS lifecycle proof lives in `sns_lifecycle.rs` |
| `real_sns_governance_direct_empty_state_lists_no_neurons_or_proposals` | `tests/e2e_real_canisters/src/sns_governance_setup.rs` | Real-framework PocketIC direct-governance smoke | Installs real SNS governance with an empty local state and queries real `list_neurons`, `list_proposals`, and nervous-system parameters | Direct governance install only; no voting, SNS-W deployment, or swap-created neurons |
| `real_sns_user_*_direct_governance_path` and `real_sns_dissolve_delay_boundaries_are_visible_direct_governance_path` | `tests/e2e_real_canisters/src/sns_governance_setup.rs` | Real-framework PocketIC direct-governance proof | Proves real ledger-funded SNS governance claim/refresh staking, top-up, minimum-stake enforcement, and dissolve-delay visibility with real `manage_neuron` and `list_neurons` calls | Direct governance install only; no SNS-W finalization, voting/following, dissolving-state transition, or root control |
| `real_sns_w_*` publication tests | `tests/e2e_real_canisters/src/sns_wasm_setup.rs` | Unit/static plus opt-in real SNS-W direct-call and NNS-proposal tests | Tracks the required SNS root/governance/ledger/index/swap/archive publication set and missing-Wasm failure. Ignored direct-call tests install real SNS-W, publish compressed source Wasms through direct `add_wasm`, verify `get_wasm`, wrong-hash rejection, and duplicate publication safety. `real_sns_w_governance_wasm_publication_payload_sizes_are_understood` proves the old 6,724,190-byte ingress was the decompressed governance-Wasm path. `real_sns_w_publishes_large_governance_wasm_via_gzipped_nns_proposal` and `real_sns_w_publishes_root_governance_ledger_index_swap_archive_via_nns` publish compressed `.wasm.gz` payloads through real NNS `manage_neuron(MakeProposal(ExecuteNnsFunction(AddSnsWasm)))` and verify SNS-W readback. | NNS proposal publication proof only; finalized staking/voting/IO-stack work is separate |
| `real_sns_root_control_uses_application_subnet_canister_direct_root_path` | `tests/e2e_real_canisters/src/sns_root_setup.rs` | Real-framework PocketIC direct-root smoke | Installs real SNS root with a local application-subnet dapp canister and verifies real `list_sns_canisters` sees the dapp | Direct root install only; not proof of SNS-W finalization, root proposal execution, or IO app upgrade |
| `real_sns_lifecycle_*`, `real_sns_participant_*`, `real_sns_swap_*`, `real_sns_finalized_governance_*`, `real_sns_user_*_after_sns_w_finalization`, `real_sns_*_after_finalization`, `real_sns_root_*_after_finalization` | `tests/e2e_real_canisters/src/sns_lifecycle.rs` | Real-framework PocketIC SNS-W lifecycle, ignored/opt-in | Publishes all six SNS Wasms through NNS proposals, calls real SNS-W `deploy_new_sns` with a local `SnsInitPayload`, verifies SNS-W `list_deployed_snses`, verifies root/governance/ledger/index/swap IDs, observes swap open, funds the swap escrow account on the local NNS ledger, calls real `new_sale_ticket`/`refresh_buyer_tokens`, observes `list_direct_participants`, awaits commit, calls real `finalize_swap`, observes finalized lifecycle, verifies direct-participation neurons through finalized governance `list_neurons`, checks participant controller permissions, checks `[0, 1]` second basket dissolve delays, checks multiple participants get distinct neuron IDs, checks no duplicate neuron IDs, checks limit-constrained listings are subsets of the full finalized listing, disburses a zero-delay direct-participation neuron for liquid test funds, proves normal post-finalization staking/top-up/multiple-neuron staking/minimum-stake rejection/below-two-week ineligibility/two-week eligibility/start-dissolving exclusion/stop-dissolving restored eligibility through finalized governance, queries finalized `list_proposals` and proves no-closed-proposals policy weight defaults to full participation, submits finalized motion proposals, observes proposer and no-voter ballots through finalized governance, proves following vote propagation from an explicit leader vote, closes a proposal under PocketIC time, converts finalized proposal/ballot observations into `io_reward_policy`, proves non-voter reward weight is lower than proposer reward weight, proves the configured 100 IO proposal rejection fee through finalized governance parameters and proposal records, verifies finalized root lists and controls an application-subnet dapp, executes a finalized governance `UpgradeSnsControlledCanister` proposal that changes the registered dapp module hash through root, and verifies a separate application-subnet non-dapp is neither listed nor controlled by finalized root | Does not yet prove broad stream-manager reward-snapshot consumption or stream-manager real ledger/index processing |
| `io_real_stack_*`, `io_stream_manager_real_finalized_sns_list_neurons_updates_active_staked_io`, `io_stream_manager_real_jupiter_deposit_scanned_from_real_icp_index`, `io_stream_manager_real_two_week_maturity_5_icp_issues_exact_backed_reward_pool`, `io_stream_manager_real_two_week_maturity_rewards_only_eligible_stakers`, `io_stream_manager_real_sns_topup_increases_active_staked_io`, `io_stream_manager_real_redemption_pays_icp_on_real_local_ledger` | `tests/e2e_real_canisters/src/io_protocol_real_stack.rs` | Real-framework PocketIC finalized-SNS IO install, unit/static plus ignored/opt-in | The unit/static tests build IO install args from finalized SNS canister IDs and reject production fiduciary, DevMainnet, protected canister, and protected neuron values before installation. The ignored install path deploys and finalizes a real local SNS through SNS-W, then installs `io_stream_manager`, `io_nns_neuron_manager`, and `io_historian` Wasms on the application subnet with local NNS ledger/governance and finalized SNS ledger/index/governance IDs. The finalized SNS index is configured both as explicit SNS evidence and as the stream-manager IO scanner index, using the real ICRC index account-history response shape. `io_stream_manager_real_finalized_sns_list_neurons_updates_active_staked_io` installs debug IO Wasms for local evidence, calls stream-manager `debug_tick`, consumes finalized SNS governance `list_neurons`, verifies the real SNS index scanner path does not corrupt state when no redemption traffic is present, and verifies `active_staked_io_e8s` equals the finalized non-dissolving two-week-or-longer SNS neuron stake. `io_stream_manager_real_jupiter_deposit_scanned_from_real_icp_index` funds the stream-manager protocol reserve with finalized SNS ledger tokens, moves 100 ICP through the local NNS ledger from a Jupiter-labelled subaccount to the stream-manager deposit account, waits for the real local ICP index, and verifies stream-manager scans one deposit, issues exactly 60 IO from the real SNS ledger reserve, records 40 ICP two-year principal plus 60 ICP liquid reserve, and does not replay the issuance. `io_stream_manager_real_two_week_maturity_5_icp_issues_exact_backed_reward_pool` grants the stream-manager finalized governance visibility over participant neurons, consumes finalized SNS `list_neurons` for reward snapshots, scans a 5 ICP two-week maturity deposit through the local ICP ledger/index, records the 2 ICP stake-accounting and 3 ICP liquid split, and transfers exactly 3 IO of backed reward pool to finalized-SNS-ledger reward accounts with successful journal entries. `io_stream_manager_real_two_week_maturity_rewards_only_eligible_stakers` creates normal post-finalization finalized-SNS staking neurons, starts one otherwise eligible neuron dissolving through real finalized governance, then proves the non-dissolving neuron receives a positive real SNS ledger reward transfer while the dissolving neuron is absent from the reward journal and receives zero. `io_stream_manager_real_sns_topup_increases_active_staked_io` creates a normal post-finalization finalized-SNS staking neuron, consumes it through stream-manager governance refresh, tops up the same neuron through finalized SNS governance, and proves stream-manager active stake increases by the exact top-up amount. `io_stream_manager_real_redemption_pays_icp_on_real_local_ledger` funds a finalized SNS IO redemption through the real SNS ledger/index, scans it through stream-manager, pays ICP on the local NNS ledger, returns redeemed IO minus the SNS ledger fee to the stream-manager protocol reserve account on the finalized SNS ledger, records the fee in the operation journal, and proves replay does not process a second redemption. | Does not yet prove current-rate redemption variants, participation-weighted reward allocation differences, historian real-source ingestion, frontend browser behavior, or root-mediated IO canister upgrades |
| Brief blocker registry and static honesty checks | `tests/e2e_real_canisters/src/brief_blockers.rs` | Unit/static coverage registry | Registers explicit test names for remaining normal staking, voting, stream-manager real stack, real-stack upgrade, historian, and local rehearsal proof targets. The frontend honesty entries are source-backed static tests over the actual frontend template, browser source, README, and declaration paths for not-live copy, local-only evidence wording, historian-only imports, no value-moving declarations, non-zero fallback behavior, and text-only rendering. | Remaining registry entries are blocker assertions, not real framework proof; frontend coverage is static and does not replace browser E2E |
| `real_canister_e2e_icp_to_io_stake_reward_redemption` | `tests/e2e_real_canisters/src/lib.rs`, `tests/e2e_real_canisters/src/exact_economics.rs` | Real-framework PocketIC opt-in real-ledger exact-economics | With pinned SNS ledger/index Wasms: proves Jupiter Faucet deposit, 40/60 backed issuance, holder rate compounding, staker reward allocation, real ledger reward transfers, redemption, ICP payout, and index history against real ICRC ledger/index canisters | Not normal SNS neuron staking, not real SNS governance/root, not real NNS maturity mechanics |

Explicit current gaps:

- real SNS neuron staking/top-up/multiple-neuron staking/minimum-stake rejection, below/two-week dissolve-delay eligibility, start-dissolving exclusion, and stop-dissolving restored eligibility are proved behind SNS-W-finalized governance.
- real SNS governance maturity/rewards are not proved.
- real SNS governance yes/no voting, following vote propagation, no-closed-proposals default participation, proposal rejection fee observation, and non-voter lower participation weighting are proved behind finalized governance; stream-manager now consumes finalized governance for active stake and one exact backed two-week reward pool, but broader participation-weighted reward variants remain unproved.
- real SNS index account history is proved only by opt-in `e2e-real-canisters` ignored tests when pinned SNS ledger/index Wasms are supplied; it is not proved in default CI.
- real SNS root finalized control is proved for listing/controller transfer of an application-subnet dapp, finalized governance/root-mediated dapp upgrade, and non-dapp exclusion.
- IO canisters install on the application subnet against finalized local SNS IDs. Stream-manager now consumes finalized SNS governance `list_neurons` for active stake, processes a Jupiter Faucet deposit through the real local ICP ledger/index plus finalized SNS ledger reserve, processes one 5 ICP two-week maturity into an exact 3 IO backed reward-pool transfer through the finalized SNS ledger, excludes a real finalized dissolving neuron from reward recipients, and processes a real finalized-SNS IO redemption with local ICP payout plus SNS IO return to reserve. Current-rate redemption variants and broader participation-weighted reward allocation through stream-manager remain unproved.
- IO APY increase from real SNS staked IO is not proved.
- full ICP -> IO -> stake -> APY -> redemption E2E with real SNS ledger/index/governance/root is not proved.


## Current Real-Framework Stride

`tools/scripts/run-real-framework-e2e` is the opt-in local operator path for pinned real framework artifacts. It fetches/verifies/decompresses configured artifacts, runs the real ledger/index tests, the PocketIC ICP-feature SNS-W bootstrap proof, gzipped NNS proposal publication for all six SNS Wasm slots, SNS-W deploy/swap-open/participation/commit/finalization/finalized-governance/finalized staking/finalized no-closed-proposals weighting/finalized yes/no voting/finalized following/finalized non-voter weighting/finalized-root tests, builds local debug IO Wasms, installs IO canisters against the finalized local SNS with local ICP index and finalized SNS index scanner principals configured, verifies stream-manager finalized-governance active-stake refresh plus no-traffic local ICP/SNS index scan safety, proves real local ICP index deposit scanning and finalized SNS reserve issuance for a 100 ICP Jupiter Faucet deposit, proves one finalized-SNS IO redemption scan with local ICP payout and SNS IO reserve return, and runs the real-ledger exact-economics E2E. Participation-weighted stream-manager reward allocation plus real reward scanner processing remain separate implementation work.
