# Simplified real-canister PocketIC plan

Required stream coverage installs the stream manager beside a pinned real SNS ICRC ledger and a canonical local ICP ICRC-1 ledger. It proves Paused install, readiness, approval, direct reserve pull, separate payout and commit, idempotency, upgrades, stale callbacks and exact stuck-transfer proofs.

Required NNS coverage installs the manager as the local neuron controller and tests Jupiter, direct maturity and one unwind child when pinned artifacts permit. Missing source-built official SNS evidence remains an explicit rehearsal blocker and does not authorize mocks as proof.
