# IO remaining work register

This register describes the implementation lineage beginning at reviewed source baseline `e9abd154b9661a343fe483128e505248c00dbd37` on `p0-simplified-execution`, plus the later commits that update this file. Release evidence records its own exact source-finalization commit rather than treating an ancestor as equivalent. The protected-NNS implementation tranche, controlled real Jupiter path, controlled real two-year maturity path, and combined real SNS/NNS lifecycle are complete in the focused PocketIC gates recorded by the coverage matrix. Historical material under `docs/research/pre-simplification/` is evidence only.

Categories are: A stale completed claim, B implementation work, C local/tooling work, D external/upstream blocker, E product/governance decision, F separately authorized mainnet action, and G intentional launch constraint.

| Item | Category | Blocks launch | Current evidence and exact completion evidence | Local Codex | Product approval | Mainnet authorization |
| --- | --- | --- | --- | --- | --- | --- |
| Protected NNS, Jupiter, two-year maturity, and combined real lifecycle | A | No | Complete in `docs/testing/e2e-coverage-matrix.md`; remove any document that still calls these paths missing. | Yes | No | No |
| Production DID wording | A | No | Production managers expose reviewed narrow methods. “Constructor-only” is historical and has been removed from current wiring/template prose. | Yes | No | No |
| Exact release source provenance | B | Yes | The release model builds a detached worktree of one exact source-finalization commit and records artifacts separately. Completion requires provenance-correct artifacts from the final tranche source commit and successful `verify_artifacts`/`verify_release`; ancestor-only manifests are rejected. | Yes | No | No |
| Official local SNS bootstrap, SNS-W deployment, swap finalization, discovery, reserve proposal, ledger negatives, index and controllers | C | No | Reproduced locally from IC `4320fdf2e613844eabae1927b1a23b98da3a7bc6`; completion requires a sanitized package passing all four local evidence validators. | Yes | No | No |
| Candidate Governance with official-base Root | D | Yes | Candidate Governance accepts the local upgrade proposal, but official-base Root traps decoding `ChangeCanisterRequest` with `Subtyping error: unit_variant`. Completion requires a reviewed mutually compatible Governance/Root release or reviewed multi-component candidate bundle. | No | No | No |
| Maintained local chunk-store creation | D | Yes for official dapp-upgrade rehearsal | Source CLI store creation fails because the bootstrapped cycles ledger is not authorized to deploy on the target application subnet. Completion requires an upstream bootstrap/CLI fix and an executed Root-mediated IO dapp upgrade. | No | No | No |
| SNS-governance lifecycle activation | C | Yes | The architecture and authenticated `set_paused` methods already establish SNS Governance as lifecycle authority. Both managers expose matching read-only proposal validators. Completion requires registration and execution through a real local SNS-W-created Governance stack; no monetary-policy or authority decision remains. | Yes | No | No |
| Completed committed rehearsal package | C | Yes | No completed package is claimed. Completion is the exact required file set plus `validate_local_sns_rehearsal`, `validate_local_sns_ledger`, `validate_local_sns_scripts`, and `validate_local_sns_committed_evidence` passing without fabricated archive or upgrade evidence. | After upstream fixes | No | No |
| Archive transition | G | No | Local Root and ICRC-3 discovery returned no archive and the nine-block workload created no archived range. Completion is either a practical real archived range or explicit no-transition evidence accepted by the validator. | Yes | No | No |
| Lifecycle source profile | C | No | Implemented as a thin adapter that invokes the restartable official rehearsal phases with the resolved bundle. Completion evidence is a successful same-source candidate lifecycle run against a maintained loopback topology. | Yes | No | No |
| Required PocketIC in ordinary PR CI | C | No | `.github/workflows/test.yml` provisions exact PocketIC 14.0.0 from its immutable HTTPS release URL and verifies decompressed SHA-256 `f5009e61bcbff297435a67a8ef9fc02178ebb9ab3ee1ec3ac81f4fc3d49319c4` before required tests execute. Completion evidence is a successful hosted workflow run. | Yes | No | No |
| Real transport-fault injection | D | No for implementation correctness; yes for final external matrix | Split, merge, child-disbursement, and Jupiter ambiguity need fixture-level injection. Do not add production state merely to emulate transport faults. | No | No | No |
| Non-1.0 maturity modulation | D | No for implementation correctness; yes for final external matrix | Current pinned fixture does not produce an adverse valid modulation. Completion requires a reviewed fixture/configuration that does. | No | No | No |
| Historian production adapters | B | Yes for production monitoring | Read model and source DTOs exist; completion is the bounded work order in `historian-monitoring-work-order.md`. Historian remains rebuildable and non-authoritative. | Yes | No | Mainnet reads require separate authorization |
| Final SNS/tokenomics/controllers/accounts/hashes/IDs | E | Yes | Values remain in `product-decision-register.md`; completion requires explicit user/governance decisions and later audit. | No | Yes | No |
| External security audit | D | Yes | Internal validation is not an external audit. Completion requires an independent reviewed report against the final release tree and launch configuration. | No | Yes | No |
| Existing protected mainnet canister/neuron audit | F | Yes | Inspection or mutation of the protected canister and neuron was not performed. Completion requires a separately authorized, scoped mainnet audit. | No | Yes | Yes |
| Mainnet testflight, install, controller handoff, activation | F | Yes | IO remains Paused and reserved canisters remain inert. Completion requires separately reviewed commands and explicit authorization for each mainnet action. | No | Yes | Yes |

The words TODO, placeholder, blocked, future work, scanner, and cohort may still occur in templates, tests, error variants, historical-disposition documents, and explicit constraints. Those occurrences do not revive scanner/cohort architecture. Template placeholders are product decisions, and error text such as “missing settlement” describes fail-closed runtime states rather than unfinished code.
