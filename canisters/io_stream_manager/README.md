# io_stream_manager

Main IO monetary-policy canister. It classifies ICP input streams, applies the 40/60 split, issues backed IO from protocol reserve when appropriate, handles redemptions, and computes IO SNS-staker entitlement.
